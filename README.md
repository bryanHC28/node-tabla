# Notas:

Este es mi primer programa en node
``` 
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     base del numero a multiplicar               [number] [required]
  -l, --listar   para listar la tabla de multiplicar
                                           [boolean] [required] [default: false]
  -h, --hasta    para saber hasta que numero iterar          [number] [required]

```